{
  "name": "web-backend",
  "version": "1.0.0",
  "description": "backend for 143",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "test": "NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../143 && npm run build && cp -r dist ../web-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibiuld && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.3",
    "morgan": "^1.10.1"
  }
}
